import type { FS } from "@techstark/opencv-js/dist/src/types/emscripten";
import type { CV } from "@techstark/opencv-js/dist/src/types/opencv";
declare global {
    var cv: CV & {
        FS: FS;
    };
}
